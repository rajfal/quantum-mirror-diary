---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import IndexRenderer from '../../components/IndexRenderer.astro';

const entries = await getCollection('manual');
entries.sort((a, b) => b.data.date.localeCompare(a.data.date)).reverse();
---

<BaseLayout>
  <IndexRenderer 
  entries={entries}
  title="Manual Archive"
  icon="🧪"
  basePath="manual"
  collectionType="manual"
/>

</BaseLayout>
